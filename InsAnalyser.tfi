


// Instantiate the module
InsAnalyser instance_name (
    .IR(IR), 
    .opcode(opcode), 
    .funct(funct), 
    .rs(rs), 
    .rt(rt), 
    .rd(rd), 
    .shamt(shamt), 
    .isBranch(isBranch), 
    .isLoad(isLoad), 
    .isStore(isStore), 
    .isALUR(isALUR), 
    .isALUImm(isALUImm), 
    .isNop(isNop)
    );


