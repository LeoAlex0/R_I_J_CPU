


// Instantiate the module
IDSeg instance_name (
    .clk(clk), 
    .rst(rst), 
    .NPCi(NPCi), 
    .IR(IR), 
    .WBFlag(WBFlag), 
    .WBAddr(WBAddr), 
    .WBVal(WBVal), 
    .NPCo(NPCo), 
    .IRo(IRo), 
    .A(A), 
    .B(B), 
    .Imm(Imm)
    );


